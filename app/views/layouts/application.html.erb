<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="shortcut icon" href="http://integravalencia.com/wp-content/uploads/2017/02/favicon.png">

    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body style="background-color: grey;">
      <div class="row column" style="background-color: white;">
        <% flash.each do |key, value| %>
          <div class="alert alert-<%= key %>"><%= value %></div>
        <% end %>
        <div class="row">
          <div class="medium-2 columns" style="margin-bottom: 1em;">
            <img src="http://integravalencia.com/wp-content/uploads/2014/02/copy-logo-Integra.jpg" width="auto">
          </div>
          <div class="medium-10 columns medium-text-right" style="color: #8a8a8a;">
            <% if current_user %>
              Usuario conectado: <span style="color: #767676;"><%= current_user.name %></span> | <%= link_to "Desconectar", destroy_user_session_path, style: "color: #cc4b37;", method: :delete %>
            <% end %>
          </div>
        </div>
        <%= yield %>
      </div>
    </div>
  </body>
</html>
